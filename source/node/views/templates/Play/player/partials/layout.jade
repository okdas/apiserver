section.view-inner(ng-if='"ready"==state'): block section

    .container: block section-container

        .row
            .col-12.section-head
                block section-head
                    .section-head-bar(style='border-bottom:1px solid #eeeeee; margin-bottom:30px;')
                        h1.section-head-title
                            block section-head-title

                        block section-head-nav
                            nav.section-head-nav

                            nav.section-head-nav.pull-right
                                a(href='#/karma') Карма: {{player.balance}}

        .row
            .col-12.col-sm-3.col-lg-3.col-push-9
                block section-side

            .col-12.col-sm-9.col-lg-9.col-pull-3
                block section-body

    .dialog(ng-if='dialog.overlay')
        .dialog-bg(ng-click='hideDialog()')
        .container: .row: .col-12.dialog-container: block dialog
    .dialog-overlay(ng-if='dialog.overlay')



section.view-inner(ng-if='"error"==state'): block error
    .container
        .row
            .col-12 Ошибка! {{error}}
